<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- NSD multicast required for LAN device discovery -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <!-- Google account access for Drive sync -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- Program reminder notifications (required at runtime on API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- TV Features (not required so the same APK installs on phones/tablets) -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <package android:name="com.android.tv.settings" />
        <!-- Explicit package visibility for Plex — without this, resolveActivity() on the
             plex://play deep link (PlexDeepLink.kt) can silently fail package-visibility
             filtering on Android 11+ even when Plex is installed, falling back to a plain
             app launch instead of jumping to the specific title. -->
        <package android:name="com.plexapp.android" />
    </queries>

    <application
        android:name=".ArflixApplication"
        android:allowBackup="false"
        android:banner="@drawable/app_banner"
        android:extractNativeLibs="false"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locale_config"
        android:theme="@style/Theme.ArflixTV"
        android:usesCleartextTraffic="true">

        <profileable
            android:shell="true"
            tools:targetApi="q" />

        <meta-data
            android:name="io.sentry.auto-init"
            android:value="false" />

        <!-- Main Activity - Compose Host -->
        <activity
            android:name=".MainActivity"
            android:banner="@drawable/app_banner"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden"
            android:exported="true"
            android:hardwareAccelerated="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ArflixTV.Splash">

            <!-- TV Launcher (app grid / banner row) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- Mobile/Tablet Launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: open Xadarr directly on the Settings screen -->
            <intent-filter>
                <action android:name="com.xadarr.tv.OPEN_SETTINGS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <!--
          Launcher-mode activity — disabled by default so it does NOT disturb
          the existing default home app on install. Enabled at runtime only
          when the user turns on "Launcher Mode" in Settings, which makes
          Xadarr appear in the system "Choose home app" picker. A real
          <activity> is required — activity-alias is not indexed by the
          Android TV home app picker.
        -->
        <activity
            android:name=".LauncherActivity"
            android:enabled="false"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.ArflixTV">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Disable default WorkManager initialization for Hilt -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <meta-data
                android:name="androidx.emoji2.text.EmojiCompatInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <meta-data
                android:name="io.github.jan.supabase.gotrue.SupabaseInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <meta-data
                android:name="androidx.lifecycle.ProcessLifecycleInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <meta-data
                android:name="com.russhwolf.settings.SettingsInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!--
          PackageInstaller session callback receiver for the in-app APK updater.
          Without this receiver, STATUS_PENDING_USER_ACTION callbacks from the system
          installer are dropped and the install never actually happens — which was the
          root cause of issues #116, #99, #75 where the updater downloaded but never
          installed the APK on Android 7+ devices.
          The action is scoped to ${applicationId} so it's unique per flavor/install.
        -->
        <receiver
            android:name=".updater.ApkInstallReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="${applicationId}.INSTALL_COMPLETE" />
            </intent-filter>
        </receiver>

    </application>

</manifest>


